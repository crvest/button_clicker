// remove element when click
function hide(element) {
    element.remove();
}

// change login button text
function logout(element) {
    element.innerText = "log out"
}

// allert when like button is clicked
function like(element) {
    alert("ninja was liked");
}